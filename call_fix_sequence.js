const https = require('https');
const token = process.env.API_KEY; // Actually the user will need to run this with a proper admin token
// It's easier if I just run it using an empty commit to force deploy, wait it is deployed.
